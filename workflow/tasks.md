# Tasks

> Managed by the Architect agent. Each task is processed by the workflow loop.
> Format: `- [ ] Task description` (unchecked = pending, `- [x]` = done)

<!-- Add tasks below. The orchestrator processes them top-to-bottom. -->
